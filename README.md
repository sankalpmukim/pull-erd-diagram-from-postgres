# Pull ERD Diagram from postgres

Use prisma's ecosystem to generate ERD diagrams. 

## Setup

### Environment file
Set `DATABASE_URL` to a **Postgres** database URL.

### Dependencies
Install dependencies

```
npm install
```